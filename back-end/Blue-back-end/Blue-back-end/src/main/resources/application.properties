spring.application.name=blue

# Server configuration
server.port=${PORT:8080}

# H2 Database
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:test}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.show-sql=true
# spring.jpa.hibernate.ddl-auto=update

# PostgreSQL Database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/blue}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${POSTGRES_USER:quang}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${POSTGRES_PASSWORD:quang}}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}

# Stock API
alphavantage.api.key=TW10G2S77NID2ZCA
alphavantage.api.url=https://www.alphavantage.co/query

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true